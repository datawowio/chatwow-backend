meta {
  name: refresh
  type: http
  seq: 2
}

post {
  url: {{url}}/v1/auth/refresh
  body: none
  auth: inherit
}

headers {
  x-device: bruno
}

script:post-response {
  bru.setEnvVar('token',res.body?.data.token)
}
