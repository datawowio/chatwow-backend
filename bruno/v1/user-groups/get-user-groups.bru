meta {
  name: get-user-groups
  type: http
  seq: 2
}

get {
  url: {{url}}/v1/user-groups/:id?includes=userGroups,userGroups,manageUsers
  body: none
  auth: inherit
}

params:query {
  includes: userGroups,userGroups,manageUsers
}

params:path {
  id: 
}
